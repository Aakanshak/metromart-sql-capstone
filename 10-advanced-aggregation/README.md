# Advanced aggregation

GROUPING SETS requests only board-required totals. ROLLUP follows an ordered hierarchy. CUBE creates every dimension combination. FILTER keeps multiple KPI conditions readable within one scan. GROUPING flags distinguish subtotal nulls from data nulls.
