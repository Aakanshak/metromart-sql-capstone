# Pivoting and reshaping

Manual FILTER aggregation is explicit and gives stable types. tablefunc crosstab is shorter for larger matrices but requires a declared output signature and extension. LATERAL VALUES normalizes a wide target table without repeated UNION scans.
