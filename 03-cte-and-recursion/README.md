# CTEs and recursion

The readable pipeline makes grain changes reviewable. Recursive hierarchy traversal rolls leaf sales through any number of parents. The recursive date spine returns all 1,096 dates and exposes the nine intentionally missing order dates as zero-revenue coverage gaps.

CTEs are used as named transformations, not automatic performance optimizers; PostgreSQL may inline non-materialized CTEs.
